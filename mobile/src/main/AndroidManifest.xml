<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.abrarsyed.parkd.myapplication" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main"
            android:label="@string/title_activity_main" >
        </activity>
        <activity
            android:name=".timeAdd"
            android:label="@string/title_activity_time_add" >
        </activity>
        <activity
            android:name=".confim"
            android:label="@string/title_activity_confim" >
        </activity>
        <activity
            android:name=".confirm2"
            android:label="@string/title_activity_confirm2"
            android:parentActivityName=".timeAdd" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="me.abrarsyed.parkd.myapplication.timeAdd" />
        </activity>
    </application>

</manifest>
